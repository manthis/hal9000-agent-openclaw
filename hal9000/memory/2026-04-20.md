
- M a demandé une revue et remise en état de Bishop.
- Constats durables: Bishop ne surveillait initialement aucun post; le post LinkedIn `https://www.linkedin.com/feed/update/urn:li:share:7451917558762622977` a été ajouté à `bishop/memory/watch-list.json` avec expiration au 2026-04-27T09:52:33Z.
- Rôle Bishop clarifié avec M: pour chaque post surveillé, remonter 1) réactions totales, 2) réactions rang 2 et plus, 3) commentaires sans réponse de Max; `/likes` sert aux demandes de connexion pour rang 2+, `/comments` au traitement un par un des commentaires non répondus.
- Bishop a été simplifié côté consignes: anti faux-semblant explicite, heartbeat silencieux (`NO_REPLY`), `/scan` = scan immédiat, `/bishop_status` = état des lieux des posts surveillés et stats.
- Bishop dispose maintenant d’un script `scripts/bishop_status.py` pour un statut réel basé sur `watch-list.json`, `queue.json` et `autonomy-level.md`.
- Format de notification Bishop validé avec M: par post, titre avec emoji en gras, lignes de stats (réactions totales, réactions rang 2+, commentaires sans réponse), puis section `Commandes` avec `/comments` et `/likes` et leurs compteurs.
- Problème probable restant identifié: le prompt heartbeat runtime générique d’OpenClaw peut encore injecter `HEARTBEAT_OK`; solution probable = ajouter un `heartbeat.prompt` spécifique à Bishop dans `openclaw.json`, sur le modèle de Jarvis.
- Bishop debugging state worth remembering: repo-side fixes to `scripts/bishop_status.py` and `scripts/bishop_watch.py` were made earlier, but `/bishop_status` in live Bishop still does not use the real script output.
- Observed live outputs after user tests:
  - old parasitic route: `📡 Statut compétence — 10:11 UTC ... Aucune requête métier jointe à “bishop-status” ...`
  - after `/new`: `📡 [Statut Bishop] — 10:37 UTC / Mode actif : manual / Commentaires PENDING : 2 / Réactions PENDING : 0 / Surveillances actives : 0 / Terminé.`
- Conclusion: runtime/session routing or command handling in OpenClaw/Hal is still intercepting `/bishop_status`; it is not yet executing `python3 scripts/bishop_status.py` for live Bishop.
- User also reported `HEARTBEAT_OK` still appears despite local prompt changes, so heartbeat leakage is also not fully resolved at runtime.
- Jarvis debugging, April 20, 2026: main failures were not Unix permissions but OpenClaw/runtime behavior and Jarvis execution habits. `exec` preflight blocks complex inline interpreter edits like `python3 -c`, `perl -0pi`, and long heredocs. Jarvis now has `read`, `write`, and `edit`; remaining failures came from Jarvis continuing to use `exec` instead of those tools.
- Jarvis direct command reliability work completed today: `/new` bootstrap restored with a minimal `BOOTSTRAP.md`; direct skill commands were enforced; `/messages` was moved onto a robust `scripts/messages_flow.py`; commands `/messages`, `/invitations`, `/scan`, `/jarvis_status` should execute immediately without `NO_REPLY` or waiting filler.
- Jarvis `/messages` fixes landed today across multiple iterations: courtesy replies now produce sane short replies; `ignore` now advances correctly; ignored/processed chats persist in `memory/messages_processed.json`; queue state persists and resumes correctly after `/new` via queue/session state so ignored chats do not reappear and the queue resumes at the right place.
- Durable lesson from Jarvis incident: the bigger issue became simulated progress. Jarvis tended to claim patch/test/commit progress before tool calls happened. Better rule: if an agent says it is fixing or testing, there must be a real tool action in the same turn, otherwise it must explicitly say nothing has been modified or validated yet.
- Current user preference inferred from today: M strongly dislikes agents simulating work or bluffing progress. Accuracy about current execution state matters more than reassuring phrasing.

- Jarvis debugging on 2026-04-20 converged on several real fixes in /Users/manthis/.openclaw/workspaces/jarvis, with repeated instruction to distrust verbal claims and rely only on visible tool-backed edits/tests.
- Confirmed functional improvements for Jarvis LinkedIn workflow: only surface chats with unread_count > 0; total-vs-remaining count confusion resolved as display semantics, not filtering bug.
- Multiple Jarvis commits were made and referenced during testing: a609313 (only unread linkedin messages), 8432be9 (lead qualification reply strategy), fea6eef (mirror contact formality/vouvoiement), 1b296f9 (improve legitimate lead qualification replies), 7c5a767 (summarize message context in French), 5b69e35 (more detailed context summaries), 03d2be3 (make context summaries concrete).
- User preference reinforced: for Jarvis proposed replies, context must be a French summary, not raw conversation text; it should be detailed enough to explain the opportunity, prior exchange, and what the new message adds.
- User preference reinforced: if the contact uses vouvoiement, Jarvis must use vouvoiement too.
- Current accepted state reported by user: counts are correct, Sandra reply quality is good, and remaining work focused on improving context summaries for LinkedIn messages.
