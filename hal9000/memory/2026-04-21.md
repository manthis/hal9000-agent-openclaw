- Leçon durable HAL: après `/new`, relire et réappliquer strictement le contrat de voix; si un bloc de réflexion final est utilisé, il doit conserver des glyphes glitchés/diacritiques visibles et des tags système corrompus, pas une version lissée.
## 06:43 UTC - Bishop multi-watch bug diagnosed and fixed
- Root cause: Bishop could scan multiple LinkedIn posts but lacked a robust persistence path to append/update new watched posts in `memory/watch-list.json`.
- Added durable fix in Bishop workspace: normalized watch-list handling, `upsert_watch(...)` in `scripts/bishop_watch.py`, and helper `scripts/add_watch.py` with deduplication by post URN / post ID / post URL.
- Validation completed with a temporary test watch, duplicate check, then cleanup.
- Bishop: fixed status inconsistency by aligning `bishop_status.py` with scan-backed queue state before rendering counts; committed and pushed as `5670745` (`Align bishop status with scan-backed queue`).
- Jarvis: corrected misrouting of QA/AI peer discussion messages that were wrongly treated as opportunity qualification; committed as `5df1c99` (`Handle QA and AI peer exchanges in Jarvis replies`).
- Jarvis: reclassified soft product pitches after connection as `📢 Prospection` instead of `✅ Légitime`; committed as `5c82474` (`Classify soft product pitches as prospection in Jarvis`).
- User preference / desired Jarvis behavior: for prospection, proposed reply should be a polite discard message by default, while `ignore` remains a separate explicit option.
- User wants a future Jarvis learning loop for manual recategorization of messages (e.g. `cat arnaque`, `cat spam`, `cat prospection`, `cat légitime`) backed by local memory so the assistant can learn from corrections.
- Bishop issue identified: repeated draft replies on the same post/comment feel robotic; desired behavior is unique, human-sounding replies instead of recycling the same fallback wording.
- Bishop: user wants comment suggestion engine to feel much more human, natural, and tightly adapted to the actual comment, not generic to the post theme.
- Bishop: `/comments` now expected UX = show 3 reply options with strong contrast (very short / medium / long), visibly centered comment block, reaction shown before comment, and a 2-3 sentence post summary under the title.
- Bishop: user wants repeated `/comments` calls on the same pending item to regenerate fresh suggestions each time instead of replaying the same trio, including after `/new`.
- Bishop: user preference on emoji distribution in suggested replies = very short reply should almost always include a coherent emoji, medium reply should usually have no emoji unless clearly justified, long reply should never add decorative emoji.
- Bishop: user wants more intra-family variation inside suggestion trios, including different openings/attacks, tone splits, and less deterministic fallback reuse.
- Bishop: user asked for validation UX to evolve from `OK / EDIT / SKIP` toward numbered choices `1 / 2 / 3 / EDIT / SKIP`, with intended behavior that choosing 1/2/3 should post the corresponding suggestion.
- Bishop: user wants suggested LinkedIn replies to feel like Max specifically, not just generally natural. Strong preferences: avoid opening many replies with `Oui,`; prefer openings like `ouais`, `yep`, `ouaip`, `exactement`, `bien vu`, `pas mieux` when they fit.
- Bishop: user judges current generated replies as still too professoral / lesson-giving; wants simple phrasing, personal framing (`je le lis comme ça`, `perso`), and replies that sound like an immediate reaction rather than an analysis.
- Bishop: target structure for `/comments` suggestions is a strict trio with distinct lengths: ultra-short, medium, and genuinely long. User says the previous long was really only medium.
- Bishop: user now wants the long option to be at least 2 or 3 sentences, but still natural and simple, not a medium reply with a bolted-on sentence.
- Bishop: emoji preference refined further: short replies can carry a coherent emoji for impact, medium replies should generally avoid emoji unless justified, long replies should avoid decorative emoji.
- Bishop: user especially wants humor handling improved so humorous comments get genuinely light, human replies instead of serious analytical ones.
